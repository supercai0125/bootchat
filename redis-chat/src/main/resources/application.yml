spring:
  redis:
      database: 1
      host: 47.94.19.227
      port: 6379
      password: root
#      jedis:
#        pool:
#          max-active: 8
#          max-wait: -1
#          max-idle: 8
#          min-idle: 0
      timeout: 50000

  thymeleaf:
    prefix: classpath:/templates/
    check-template-location: true
    suffix: .html
    encoding: UTF-8
    mode: LEGACYHTML5
    cache: false

server:
  #端口号
  port: 8082
  #项目名，如果不设定，默认是 /
  servlet:
      context-path: /bootchat
